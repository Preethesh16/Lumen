node_modules/
dist/
.next/
out/
build/

# Secrets — never commit. See .env.example for the required variables.
.env
.env.local
.env.*.local

# Local dev caches for upstream APIs, so test runs don't burn GDELT/ReliefWeb quota
.cache/
*.log

.DS_Store
coverage/
*.tsbuildinfo
